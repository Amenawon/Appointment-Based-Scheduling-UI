# 🗓️ Appointment Scheduling System — Frontend (React)

This repository contains the **frontend user interface** for the Appointment Scheduling System.  
The UI is built with **React** and provides functionality for managing appointments, attendees, and user authentication.

<img width="1825" height="829" alt="image" src="https://github.com/user-attachments/assets/815bf366-f0f7-4431-a43e-1d36390fbb9b" />





<img width="1835" height="804" alt="image" src="https://github.com/user-attachments/assets/e46e8379-2146-44d3-a549-66f8fa93b6bc" />


---

## 🚀 Features

- **User Authentication** — Login system with JWT-based authentication integrated with the backend API.  
- **Appointment Booking** — Create and manage appointments with title, description, date, time, duration, and location.  
- **Date & Time Selection** — Interactive date picker and dropdown-based time selector for precise scheduling.  
- **Attendee Management** — Add both registered and placeholder attendees using email input.  
- **Dynamic UI Updates** — Form state management with React hooks (`useState`, `useEffect`).  
- **Session Persistence** — Stores user tokens and email in `localStorage` for smooth user sessions.  

---

## 🧠 Tech Stack

| Category | Technology |
|-----------|-------------|
| Frontend Framework | React (with React Router) |
| State Management | React Hooks (`useState`, `useEffect`) |
| HTTP Client | Axios |
| Authentication | JWT (stored in `localStorage`) |
| Styling | Custom CSS |
| Backend API | ASP.NET Core Web API |
| Database (via backend) | SQL Server |

---

## ⚙️ Setup & Installation

### 1. Clone the repository

### 2. Install dependencies
```bash
npm install
```

### 3. Run the development server
```bash
npm start
```

